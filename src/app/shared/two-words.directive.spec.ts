import { TwoWordsDirective } from './two-words.directive';

describe('TwoWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoWordsDirective();
    expect(directive).toBeTruthy();
  });
});
